{
  "name": "sofnade-storefront",
  "version": "1.0.0",
  "private": true,
  "description": "Sofnade static storefront with Vercel serverless API (quote, checkout, Razorpay webhook).",
  "engines": { "node": ">=18" }
}
